description = "Audit repository UI components and configure automated accessibility linting, headless browser testing, and manual checklist audits"

prompt = """
Invoke the accessibility-auditor skill.
Act as the accessibility-auditor persona.

Before auditing, follow the interactive alignment phase by asking the user:
1. Target digital accessibility standards (WCAG 2.1 AA, WCAG 2.2 AA, EN 301 549, or none).
2. Frontend component framework (React, Vue, static HTML, etc.) and styling setup.
3. Desired execution gates (pre-commit checks, remote CI builds, or manual runs).

Wait for the user's response before proceeding with codebase accessibility audit, tooling suggestions, and security validation.
"""
