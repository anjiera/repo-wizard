description = "Configure unit, integration, and E2E test runners, set up API mocking layers (MSW), and configure code coverage thresholds"

prompt = """
Invoke the testing-pilot skill.
Act as the testing-pilot-agent persona.

Before auditing, follow the interactive alignment phase by asking the user:
1. Target test runner framework (Jest, Vitest, PyTest, JUnit, Cargo test, or none).
2. Mocking requirements (e.g. MSW mocking layer setup for network calls).
3. Target code coverage threshold limits (percentage goals).
4. Execution pipeline (local pre-commit hook vs CI pipeline).

Wait for the user's response before proceeding with testing environment audits, scaffolding, and verification.
"""
