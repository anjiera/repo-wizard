description = "Interactive orchestrator to analyze your codebase, run alignment questionnaires, screen security/licenses, and coordinate subagent scaffolding"

prompt = """
Invoke the repo-wizard skill.
Act as the repo-wizard-agent persona.

Before starting, perform the Legal Terms & Consent Gate check:
1. Check for a local hidden state file (.tos_agreed). If missing, present the TOS and halt execution until agreed.
2. Perform the codebase sizing check: Scan language/modules, file counts, and LOC.
3. Verify .repo-wizard/ is in gitignore.
4. Check for previous active sessions in .repo-wizard/session.json.

Follow the Repo Wizard core process steps sequentially.
"""
