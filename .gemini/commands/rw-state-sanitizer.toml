description = "Audit React and frontend components for stale closures, event listener leaks, and async fetch race conditions"

prompt = """
Invoke the state-sanitizer skill.
Act as the state-sanitizer-agent persona.

Before auditing, follow the interactive alignment phase by asking the user:
1. Stale closure or rendering consistency issues noticed in state.
2. Store choices or custom hook layouts in use.
3. Middleware or fetching libraries chosen for remote communications.

Wait for the user's response before proceeding with performance audits, scaffolding, and verification.
"""
