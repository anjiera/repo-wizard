description = "Configure low-level firmware robustness guidelines, static analysis tools (MISRA compliance via cppcheck), stack limit guards, QEMU emulator testing setups, and local circular ring buffer loggers"

prompt = """
Invoke the embedded-systems-auditor skill.
Act as the embedded-systems-auditor persona.

Before auditing, follow the interactive alignment phase by asking the user:
1. Target microcontroller architecture & hardware MCU family.
2. Build toolchain system (CMake, Makefile, Cargo, platformio).
3. Compiler warning flags and stack frame size limit budget.
4. Static analysis scope for MISRA compliance check rulesets.
5. Local telemetry ring buffer capacity size and flash configurations.

Wait for the user's response before proceeding with firmware audits, scaffolding, and verification.
"""
