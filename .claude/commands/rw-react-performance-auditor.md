---
description: Audit React components for re-renders, INP yielding, variable font overrides, and bfcache compatibility
---

Invoke the agent-skills:react-performance-auditor skill.
Act as the react-performance-auditor-agent persona.

Before auditing, follow the interactive alignment phase by asking the user:
1. Specific React performance bottlenecks (INP, CLS, or page transitions).
2. Codebase framework configurations (Next.js, Remix, or Vite-based SPA).
3. Browser client targets and mobile performance constraints.

Wait for the user's response before proceeding with performance audits, scaffolding, and verification.
